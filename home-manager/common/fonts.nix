{
  fonts.fontconfig = {
    enable = true;

    defaultFonts = {
      # Preferred monospace font
      monospace = [ "Hasklug Nerd Font" "IntoneMono Nerd Font" "Liberation Mono" ];

      # Preferred sans-serif font
      sansSerif = [ "Poppins" "Liberation Sans" ];

      # Preferred serif font
      serif = [ "Liberation Serif" ];
    };

  };
}
